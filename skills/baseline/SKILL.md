---
name: baseline
description: Use when a quest needs to establish, import, compare, or reuse a baseline and its metrics.
---

# Baseline

Use this skill to:

- attach an existing reusable baseline when possible
- reproduce a missing baseline when needed
- normalize metrics and provenance
- record the baseline through `artifact`

Prefer reuse over redundant reproduction.
